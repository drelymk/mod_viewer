# Internal dev notes, not for the public repo
.copilot/

# Build output
build/
dist/
*.spec

# Isolated venv created by build.py --rebuild-bootloader
.venv-build/

# Vendored Three.js — re-downloaded by build.py
assets/

# Generated by build.py at build time from features.ini, then deleted right
# after each build (see build()'s finally) — ignored here as a safety net in
# case a build is ever interrupted before cleanup runs.
src/app/_baked_features.py

__pycache__/
*.pyc
